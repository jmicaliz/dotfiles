# User-Level CLAUDE.md

## Python projects

For any Python work — existing repos or new projects — follow the
`python-conventions` skill (uv, pyproject.toml, Makefile targets, Ruff, mypy,
pytest, Click, loguru).